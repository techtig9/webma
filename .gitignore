node_modules/
.next/
dist/
.vercel/
*.log
*.tsbuildinfo
*.zip
.DS_Store

# Keep .env.example committed — everything else is a real or local secret.
.env
.env.local
.env.*.local

# One-off local tooling (e.g. a manual thumbnail-render script) — never
# meant to ship as part of the app, kept out of the repo entirely.
.thumbgen/
