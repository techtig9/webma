node_modules/
.next/
dist/
.vercel/
*.log
.DS_Store
*.tsbuildinfo
*.zip

# Keep .env.example committed — everything else is a real or local secret.
.env
.env.local
.env.*.local

# One-off local tooling (e.g. the real-template-thumbnail render script) —
# never meant to ship as part of the app, kept out of the repo entirely.
.thumbgen/

# Generated output of scripts/templates/regenerate.mjs — derived from
# categories.mjs + pages_by_template.json, regenerate via `node regenerate.mjs`.
scripts/templates/regenerated.json
